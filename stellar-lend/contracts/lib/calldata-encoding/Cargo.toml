[package]
name = "calldata-encoding"
version = "0.1.0"
edition = "2021"
publish = false

[lib]

[dependencies]
soroban-sdk = { workspace = true, features = ["alloc"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
