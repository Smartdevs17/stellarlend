[package]
name = "shared-math"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { version = "21.5", features = ["contracttype", "testutils"] }
soroban-contract-sdk = "21.5"

[dev-dependencies]
proptest = "1.4"
