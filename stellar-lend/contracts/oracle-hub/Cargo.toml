[package]
name = "oracle-hub"
version = "0.1.0"
edition = "2021"
description = "Centralized OracleHub contract with feed prioritization, median aggregation, outlier rejection, and emergency freeze"

[dependencies]
soroban-sdk = { workspace = true }
soroban-token-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
